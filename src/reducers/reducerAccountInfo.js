const initialState = {
    phoneNumber: null,
    parentId: null,
    auth: null
};

const reducerAccountInfo = (state = initialState, action) => {
    return state;
};

export default reducerAccountInfo;